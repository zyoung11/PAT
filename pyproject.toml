[project]
name = "PAT"
version = "0.2.0"
description = "PAT 是一个用于简化API接口测试的Python迷你库。它基于 requests 和 rich 库，为您的API测试提供美观、易读的终端输出。"
readme = "README.md"
requires-python = ">=3.12.0"
dependencies = [
    "requests>=2.34.2",
    "rich>=15.0.0",
]
